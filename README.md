# Скрингуру

[![Stars](https://img.shields.io/github/stars/q0wqex/screenguru.svg?style=social)](https://github.com/q0wqex/screenguru/stargazers)
[![Docker Image](https://img.shields.io/badge/docker-ghcr.io-blue?logo=docker)](https://github.com/q0wqex/screenguru/pkgs/container/screenguru)
[![License](https://img.shields.io/badge/license-AGPL--3.0-orange)](LICENSE)

[🇷🇺 Русский](README.md) | [🇺🇸 English](README_EN.md)

## Описание

Легковесный и быстрый сервис для хостинга изображений, написанный на Go. Создан как простая альтернатива для тех, кто ищет минимализм и скорость.

> *Скрингуру — когда привычка к простому и красивому хостингу остается навсегда.*

## Особенности

- **🖼️ Поддержка изображений**: Загрузка и просмотр JPEG, PNG, GIF, WebP.
- **📁 Альбомы**: Организация изображений по альбомам.
- **🧹 Авто-очистка**: Автоматическое удаление старых файлов (по умолчанию через 30 дней).
- **🚀 Ультра-быстрый**: Минимальные зависимости, использование кешируемых шаблонов.
- **🐳 Docker Ready**: Полная поддержка контейнеризации и деплоя одной командой.
- **📱 Адаптивный UI**: Интерфейс подстраивается под мобильные устройства и десктоп.

## API

Сервер запускается на порту `8000` по умолчанию.

| Эндпоинт | Метод | Описание |
|----------|---------|-------------|
| `/` | GET | Главная страница / Просмотр альбомов |
| `/upload` | POST | Загрузка изображения |
| `/create-album` | POST | Создание нового альбома |
| `/delete-image` | POST | Удаление конкретного изображения |
| `/delete-album` | POST | Удаление всего альбома |
| `/delete-user` | POST | Удаление пользователя и всех его данных |
| `/changelog` | GET | Просмотр истории изменений |

## Конфигурация

Основные параметры определены в `app/config.go`:

| Переменная | Значение по умолчанию | Описание |
|----------|---------------|-------------|
| `MaxFileSize` | `10 * 1024 * 1024` (10MB) | Максимальный размер файла |
| `CleanupDuration` | `30 дней` | Срок хранения файлов до удаления |
| `CleanupInterval` | `24 часа` | Частота проверки старых файлов |
| `DataPath` | `/data` | Путь к директории с данными |
| `ServerAddr` | `0.0.0.0:8000` | Адрес и порт сервера |

## Инструкции по установке

### 1. Быстрый запуск (Docker)

Запустите сервис одной командой (создаст папку, скачает конфиг и поднимет контейнер):

```bash
mkdir -p screenguru/data && cd screenguru && curl -O https://raw.githubusercontent.com/q0wqex/screenguru/main/docker-compose.yml && docker-compose up -d
```

### 2. Ручная установка

Если вы хотите собрать проект из исходников:

1. Склонируйте репозиторий:
   ```bash
   git clone https://github.com/q0wqex/screenguru.git && cd screenguru
   ```
2. Соберите и запустите:
   ```bash
   cd app && go build -o screenguru && ./screenguru
   ```

## Настройка Reverse Proxy & SSL (Caddy)

Caddy теперь автоматически обслуживает два домена: `screengu.ru` and `dev.screengu.ru`.

1. **Caddyfile**: Находится в корне проекта. В нем настроено проксирование:
   - `screengu.ru` -> `localhost:8000`
   - `dev.screengu.ru` -> `localhost:8888`
2. **Автоматический SSL**: Caddy сам получит и обновит сертификаты для обоих доменов.
3. **Безопасность**: Приложение привязано к `127.0.0.1`.

4. **Данные**: Сертификаты сохраняются в папку `caddy_data`.

По умолчанию лимит загрузки в Caddy настроен на 10MB (`request_body_limit`).




## Структура проекта

- `/app` — Исходный код сервера на Go.
- `/app/templates` — HTML шаблоны и статические файлы (JS/CSS).
- `/data` — Хранилище изображений.
- `docker-compose.yml` — Файл для Docker.
- `changelog.md` — История изменений.

## Благодарности

- **[Remnawave](https://remna.st/)** — за отличный визуальный стиль и дизайн, ставший основой для оформления этого проекта.
